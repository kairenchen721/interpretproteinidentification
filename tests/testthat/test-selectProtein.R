

# make the grpah, decompose it, then check that it return a graph that contain it
# and its neighbours
# I think match return something when it is not in it (it would return nomatch)
# 
# also for an vertex that is not in it
test_that("multiplication works", {
  expect_equal(2 * 2, 4)
})
